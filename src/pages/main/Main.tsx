import { LayoutPage } from '../../shared/layouts';

export const Main = () => {
    return (
        <LayoutPage title='Página Inicial' toolbar={<>Barra de Ferramentas</>}>
            Testando
        </LayoutPage>
    );
};